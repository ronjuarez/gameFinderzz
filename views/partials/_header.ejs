      <!-- bootstrap -->
      <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">

      <!-- Nav CSS -->
      <link rel="stylesheet" href="/styles/nav.css" type="text/css" />
<header>
  <nav class="nav-bar">
    <a id="logo" class="navbar-brand" href="/">FryApp</a>

      <div class="non-logo">
        <a class="navbar-post-new" href="/games/new">Post a new ad</a>
        <% if(user){ %>
        <a class="navbar-faves" href="/favorites">Favorites</a>
          <form method="POST" action="/logout">
            <a class="navbar-email">Logged in as: <%= user.email %></a>
            <button type="submit" class="btn btn-primary">Logout</button>
          </form>
        <% } else { %>
          <a class="navbar-email" href="/login">Already Registered? Log In Here!</a>
       <% } %>
      </div>
  </nav>
</header>
